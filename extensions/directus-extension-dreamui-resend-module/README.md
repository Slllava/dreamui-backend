# DreamUI Resend Module

Directus module extension that displays sent emails from Resend through the local Directus endpoint.

## Requirements

- Install `directus-extension-dreamui-resend-endpoint`
- Set `RESEND_API_KEY` on the Directus server

## Features

- Sent email list with pagination
- Email detail dialog
- Settings tab with environment guidance
